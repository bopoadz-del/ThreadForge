# ThreadForge agent tools

AUTO-GENERATED by `scripts/gen_docs.py`. Do not hand-edit the table.

| Tool | Purpose |
|---|---|
| `attach_schedule` | Import schedule JSON/CSV and attach dates to work packages. |
| `build_test_packs` | Build test packs from system boundary tags. |
| `build_work_packages` | Build CWP/IWP work packages from design volumes + tags. |
| `cascade_rerun` | Re-run generators for artefacts dirtied by the last change. |
| `clash_check` | Run capsule clash detection; write output/clash/clash_report.json. |
| `co_activity_check` | Flag work packages that overlap in time and share a design volume. |
| `dexpi_coverage` | Report supported DEXPI-shaped elements vs known gaps (WALL). |
| `export_artefacts` | Write PCF, ISO, GA, routes, quantities under output/. |
| `ingest_dexpi` | Parse Proteus/DEXPI-shaped XML (or default fixture) into the topology graph. |
| `look_ahead` | List WPs/activities in the look-ahead window (optional discipline filter + CSV). |
| `maturity_check` | Refuse IFC-grade export when current maturity is below the gate. |
| `query_graph` | Query tags, lines, neighbors, volumes, or full summary. |
| `revise_pid` | Revise a tag or pipeline; records change and marks dirty artefacts. |
| `run_pipeline_stage` | Run one (or all) Pid-to-3d pipeline stages. |
