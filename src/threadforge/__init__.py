"""ThreadForge — agent-native EPC digital-thread MVP."""

__version__ = "0.1.0"

# Lazy/optional imports kept out of package import path so Render can boot
# with a bootstrap server.py before the full tree is unpacked.

__all__ = ["__version__"]
